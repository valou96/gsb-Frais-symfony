{% extends 'base.html.twig' %}

{% block title %}LigneHorsForfait index{% endblock %}

{% block body %}
    <h1>LigneHorsForfait index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Montant</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne_hors_forfait in ligne_hors_forfaits %}
            <tr>
                <td>{{ ligne_hors_forfait.id }}</td>
                <td>{{ ligne_hors_forfait.libelle }}</td>
                <td>{{ ligne_hors_forfait.montant }}</td>
                <td>{{ ligne_hors_forfait.date ? ligne_hors_forfait.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_ligne_hors_forfait_show', {'id': ligne_hors_forfait.id}) }}">show</a>
                    <a href="{{ path('app_ligne_hors_forfait_edit', {'id': ligne_hors_forfait.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ligne_hors_forfait_new') }}">Create new</a>
{% endblock %}
