{% extends 'base.html.twig' %}

{% block title %}Hello SaisieFicheFraisController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
{# <div>Fiche frais du {{ fiche_frai.dateModif }}</div>#}
<div class="example-wrapper">
    {{ form_start(formFF) }}
    <div class="row mb-3">
        <div class="form-floating">
            {{ form_row(formFF.libelleETP) }}
        </div>
    </div>
    <div class="row mb-3">
        <div class="form-floating">
            {{ form_row(formFF.libelleKM) }}
        </div>
    </div>
    <div class="row mb-3">
        <div class="form-floating">
            {{ form_row(formFF.libelleNUI) }}
        </div>
    </div>
    <div class="row mb-3">
        <div class="form-floating">
            {{ form_row(formFF.libelleREP) }}
        </div>
    </div>
    <button type="submit" class="btn btn-primary">Valider les frais forfait</button>
    {{ form_end(formFF) }}

</div>
    <div class="example-wrapper">
        {{ form_start(formHF) }}
        <div class="row mb-3">
            <div class="form-floating">
                {{ form_row(formHF.libelle) }}
            </div>
        </div>
        <div class="row mb-3">
            <div class="form-floating">
                {{ form_row(formHF.montant) }}
            </div>
        </div>
        <div class="row mb-3">
            <div class="form-floating">
                {{ form_row(formHF.date) }}
            </div>
        </div>
        <button type="submit" class="btn btn-primary">Valider les frais hors forfait</button>
        {{ form_end(formHF) }}
    </div>
{% endblock %}
