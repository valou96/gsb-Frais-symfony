

<div class="container">
    {{ form_start(form) }}
    <div class="row mb-3">
        <div class="form-floating">
            {{ form_row(form.mois) }}
        </div>
    </div>
    <div class="row mb-3">
        <div class="form-floating">
            {{ form_row(form.date) }}
        </div>
    </div>
    <button type="submit" class="btn btn-primary">Valider</button>
    {{ form_end(form) }}
</div>