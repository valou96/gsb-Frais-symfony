<form method="post" action="{{ path('app_seminaire_delete', {'id': seminaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seminaire.id) }}">
    <button class="btn">Delete</button>
</form>
