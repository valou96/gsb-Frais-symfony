{% extends 'base.html.twig' %}

{% block title %}Seminaire{% endblock %}

{% block body %}
    <h1>Seminaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seminaire.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ seminaire.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_seminaire_index') }}">back to list</a>

    <a href="{{ path('app_seminaire_edit', {'id': seminaire.id}) }}">edit</a>

    {{ include('seminaire/_delete_form.html.twig') }}
{% endblock %}
