{% extends 'base.html.twig' %}

{% block title %}Seminaire index{% endblock %}

{% block body %}
    <h1>Seminaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for seminaire in seminaires %}
            <tr>
                <td>{{ seminaire.id }}</td>
                <td>{{ seminaire.libelle }}</td>
                <td>
                    <a href="{{ path('app_seminaire_show', {'id': seminaire.id}) }}">show</a>
                    <a href="{{ path('app_seminaire_edit', {'id': seminaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_seminaire_new') }}">Create new</a>
{% endblock %}
